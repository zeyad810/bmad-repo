# Deferred Work

## Deferred from: code review of 1-6-single-stream-home-layout-today-and-backlog-sections (2026-09-22)

- Overdue Backlog tasks qualify as Today tasks via `isTodayTask`, creating drag status ambiguity (`hooks/useTasks.ts:33-46`) — deferred, pre-existing (tracked as risk `EPIC1-R02` in Epic 1 test design).
- Hardcoded priority score fallbacks in `handleQuickAdd` (`app/page.tsx:113`) — deferred, pre-existing pattern from Story 1.4.

## Deferred from: dev-story of 2-1-in-place-delete-confirm (2026-09-22)

- **[Resolved by Story 3.4, 2026-09-23: `aria-label="Add a task"` restored, along with Escape-to-clear and the submit button's "Add task" name. Specs using `getByLabel('Add a task')` are no longer broken by the label; they're still blocked by the hydration hang.]** **`QuickAddBar.tsx` is missing its documented `aria-label="Add a task"`**: Story 1.4's dev record (`1-4-quick-add-bar-and-silent-no-op-capture.md`) explicitly claims the input has `aria-label="Add a task"` and that all 27 tests (including `tests/e2e/quick-add-bar.spec.ts`, which asserts `input[aria-label="Add a task"]`) passed on all three browsers. The actual current `components/tasks/QuickAddBar.tsx` has no `aria-label` on the input at all — only `placeholder="Add a task..."`. Confirmed this is not caused by Story 2.1's changes: running the pre-existing `tests/e2e/quick-add-bar.spec.ts` in isolation on the unmodified app also fails at the same locator (`1.4-E2E-001` times out on `page.getByLabel('Add a task')`). This affects every existing Epic 1 spec that uses this locator (`quick-add-bar.spec.ts`, `single-stream-layout.spec.ts`, `task-reorder.spec.ts`, `complete-from-backlog.spec.ts`), meaning the full Playwright suite is very likely currently broken/red end-to-end regardless of Story 2.1. Needs investigation (likely a lost edit or bad merge somewhere between Story 1.4's completion and the current working tree) — out of scope to fix from within Story 2.1. Story 2.1's own new spec (`tests/e2e/delete-confirm.spec.ts`) was written to route around this by locating the input via `getByPlaceholder('Add a task...')` instead.
- **Story 2.1's own Playwright coverage (`tests/e2e/delete-confirm.spec.ts`) was authored but not executed to a passing state this session** — the full-suite run was cut short (per explicit user direction to stop investigating the dev-server/port issue and close out the story) before confirming green runs across chromium/firefox/webkit. Recommend running `npx playwright test tests/e2e/delete-confirm.spec.ts --workers=1` (and ideally the full suite, once the `QuickAddBar` aria-label regression above is fixed) before this story is treated as fully verified.

## Deferred from: dev-story of 2-2-restyle-task-editing-taskdrawer-and-taskform (2026-09-22)

- **Story 2.2's `tests/e2e/task-form-restyle.spec.ts` (5 tests) was authored but never run to green.** A chromium run failed in all 5 tests at the quick-add step. Playwright reused an existing server on port 3000 (`reuseExistingServer`), and it rendered only the sidebar with no main content. This is the same stale-server class of problem as Story 2.1. The drawer-dependent Epic 1 specs (`single-stream-layout.spec.ts` 1.6-E2E-003/004/005, `quick-add-bar.spec.ts` 1.4-E2E-001) were also not re-run. Next step: stop whatever holds port 3000, then run `npx playwright test tests/e2e/task-form-restyle.spec.ts --project=chromium`.
- **Est. Minutes `NaN` bug still open (pre-existing, Story 1.4):** an empty Est. Minutes field becomes `NaN` via `valueAsNumber`, and zod rejects it, which silently blocks every save that leaves it empty. It was deliberately not fixed in 2.2 (the AC requires unchanged validation behavior). It needs its own fix (e.g. `setValueAs: v => v === "" ? undefined : Number(v)`), after which the `fill('30')` workarounds in the E2E specs can be removed.

## Deferred from: dev-story of 1-8-complete-a-task-directly-from-the-backlog-section (2026-09-22)

- **Webkit-only flakiness in `QuickAddBar`-dependent tests**: tests in `quick-add-bar.spec.ts`, `single-stream-layout.spec.ts`, and `drag-across-divider.spec.ts` intermittently fail on the `webkit` project with `getByText(<just-added title>)` timing out (15s) after `input.press('Enter')`. Reproduced across two full-suite runs on 2026-09-22 with different subsets failing each time (3 failures on one run, 9 on an immediate re-run of the same files), indicating timing/perf flakiness rather than a deterministic break. Chromium and firefox were consistently green across both runs, as was all of `complete-from-backlog.spec.ts` (Story 1.8's own coverage, 9/9 passing on all three browsers both times). No production code was changed in Story 1.8's session, so this is pre-existing and unrelated to that story's scope — likely Next.js dev-server (`npm run dev`) compile/render latency compounding with WebKit's slower automation driver. Needs investigation (e.g. explicit `waitForResponse`/compile-warmup, or a `webServer` prod build for test runs) before it can be trusted as a hard regression gate.

## Deferred from: dev-story of 2-3-restyle-buttons-and-enforce-hierarchy (2026-09-22)

- **Story 2.3's `tests/e2e/button-hierarchy.spec.ts` (5 tests) was authored but never run to green.** The red-phase chromium run failed in all 5 at the quick-add step, against the same stale server on port 3000 (PID 4072) that blocked 2.1 and 2.2. `delete-confirm.spec.ts` (2.1-E2E-005 now drives the `Button`-based Clear archive) and `task-form-restyle.spec.ts` were not re-run either. Next step, the same as 2.2: free port 3000, then run `npx playwright test tests/e2e/button-hierarchy.spec.ts tests/e2e/delete-confirm.spec.ts tests/e2e/task-form-restyle.spec.ts --project=chromium`.

## Deferred from: code review of 2-3-restyle-buttons-and-enforce-hierarchy (2026-09-22)

- **TaskCard's armed delete state isn't styled or accessible:** the row delete's "Confirm?" state in the Completed archive is plain `zinc-600` text, which is about 2.5:1 contrast and fails WCAG AA. It also gets no emphasis, unlike Clear archive's `danger` state. Pre-existing (legacy `TaskCard`). Candidate for the 3.4 accessibility pass, or for migrating the Completed archive to `TaskRow`.
- **In-place confirm isn't announced to screen readers:** the label swap ("Clear archive" ↔ "Tap to confirm", and the row "Confirm?") has no live region, the ~2.5s auto-revert happens silently, and "Tap" is the wrong verb for keyboard users. This is the Story 2.1 `useConfirmDelete` pattern, pre-existing. It fits 3.4.
- **`failOnNativeDialog` in `tests/e2e/delete-confirm.spec.ts` throws inside a `page.on('dialog')` listener:** that doesn't reliably fail the test. It's pre-existing from Story 2.1. Collect dialogs into an array and `expect(dialogs).toHaveLength(0)` instead, as 2.3's spec will.
- **The E2E blocker may not be a stale server:** the red-phase screenshot for 2.3 (`test-results/button-hierarchy-*/test-failed-1.png`) shows the main content still on the hydration skeleton after 15s, with only the sidebar rendered. The "stale server on :3000" diagnosis used for 2.1–2.3 is unverified. Hydration may simply never complete (see `HydrationProvider`, and Story 1.10's skeleton). Worth one look before blaming the environment again.

## Deferred from: dev-story of 2-4-restyle-empty-states (2026-09-22)

- **Story 2.4's `tests/e2e/empty-states.spec.ts` (4 tests) was authored but never run to green.** The red phase failed as expected (the `empty-state` test hook was missing). After implementation, two chromium runs still failed all 4 tests. The cause is not the story code. `curl http://localhost:3000/completed` returns the new server-rendered markup (`data-testid="empty-state"`, token classes). But in the browser, the failure screenshot for a test on **`/completed`** shows the **home page's `HydrationSkeleton`** in `<main>`, which stays stuck. This strengthens 2.3's suspicion that the E2E blocker is the client never finishing hydration or routing against the dev server on :3000 (PID 4072, `next start-server.js` from this repo), not a stale build. The dependent specs (`single-stream-layout`, `hydration-skeleton`, `drag-across-divider`, `complete-from-backlog`, `delete-confirm`) were not re-run. Next step: restart the dev server (or use a prod `webServer` build for tests), then run `npx playwright test tests/e2e/empty-states.spec.ts --project=chromium` and those five specs.

## Deferred from: dev-story of 3-1-restyle-sidebar-and-bottomnav (2026-09-23)

- **The sidebar count regression (`complete-from-backlog.spec.ts` 1.8-E2E-003) wasn't verified green after the Sidebar restyle.** All 3 tests in that spec fail on chromium at the quick-add step. The spec's own helper uses `getByLabel('Add a task')`, which is the known QuickAddBar aria-label regression above. A throwaway copy that switched to `getByPlaceholder('Add a task...')` **also** timed out at `locator.click` on the input. That fits the hydration hang reported for 2.3 and 2.4 (`<main>` stuck on the skeleton against the :3000 dev server), not the nav change. The copy was deleted after the run. Story 3.1's own `tests/e2e/nav-restyle.spec.ts` (5 tests) **passed 5/5 on chromium**, because the nav is server-rendered and doesn't depend on `<main>` hydrating. Next step: once the hydration/dev-server blocker is fixed, re-run `npx playwright test tests/e2e/complete-from-backlog.spec.ts tests/e2e/nav-restyle.spec.ts`. Consider also switching `complete-from-backlog.spec.ts`'s helper to the placeholder locator, or restoring the QuickAddBar `aria-label`.

## Deferred from: dev-story of 3-2-responsive-breakpoint-and-touch-target-compliance (2026-09-23)

- **Story 3.2's task-dependent tests weren't verified green.** In `tests/e2e/responsive-layout.spec.ts`, 3.2-E2E-003 (both widths), 004 (44×44 row targets), 005 (last row clears the BottomNav), and 006 (`touch-action`, no inline styles, 44px quick-add) all time out at `locator.fill` on the quick-add input. The failure screenshot shows `<main>` stuck on `HydrationSkeleton` at 390×844, which is the same hang as 2.3–3.1 against the :3000 dev server (PID 4072). The shell around it does render correctly in that screenshot: 16px gutters, the mobile brand mark, the BottomNav, and no sidebar. The breakpoint tests that don't need hydration, **3.2-E2E-001/002, pass**, as do `nav-restyle.spec.ts` 5/5 after the `lg`→`md` move. `drag-across-divider.spec.ts` and `delete-confirm.spec.ts` weren't re-run, because they're blocked at the same quick-add step. Next step: once hydration is fixed, run `npx playwright test tests/e2e/responsive-layout.spec.ts tests/e2e/drag-across-divider.spec.ts tests/e2e/delete-confirm.spec.ts --project=chromium`.

## Deferred from: dev-story of 3-3-keyboard-accessible-reorder-fallback (2026-09-23)

- **Story 3.3's `tests/e2e/keyboard-reorder.spec.ts` (5 tests covering 3.3-E2E-001 through 007) was authored but never run to green.** Every test times out at `locator.fill` on the quick-add input, before any keyboard interaction, in both the red and the green runs. This is the same `<main>`-stuck-on-`HydrationSkeleton` hang as 2.1–3.2. The pointer regressions `drag-across-divider.spec.ts` (4) and `task-reorder.spec.ts` (1) fail at the same step. They also still use `getByLabel('Add a task')`, the deferred aria-label regression. The pure drop logic (`lib/reorder/resolve-drop.ts`) *was* verified: 10 assertions, compiled with `tsc` and run in Node from the scratchpad, cover same-section, keyboard demote, keyboard promote (↑×1 lands last, ↑×2 lands second-to-last), unchanged pointer insert-before, the empty dropzone, and the null cases. **Not verified in a browser:** the live-region text, focus restore after a cross-section move, and Escape cancel. Next step, once hydration is fixed: `npx playwright test tests/e2e/keyboard-reorder.spec.ts tests/e2e/drag-across-divider.spec.ts tests/e2e/task-reorder.spec.ts --project=chromium`.

## Deferred from: dev-story of 3-4-accessibility-pass-contrast-focus-and-semantics (2026-09-23)

- **Story 3.4's task-dependent tests weren't verified green.** In `tests/e2e/a11y-pass.spec.ts`, 3.4-E2E-002 and 004–008 time out at the quick-add step, because `<main>` is stuck on `HydrationSkeleton` (the same hang as 2.1–3.3). 003's header assertions pass, and it then times out on the quick-add placeholder. **Passing:** 3.4-E2E-001 (the accent ring on the Sidebar and BottomNav links, 3 of 3 runs) and 009 (pinch-zoom no longer disabled). `nav-restyle` 5/5 and `responsive-layout` 001/002 still pass. `quick-add-bar.spec.ts` (Epic 1) still fails, because the form never renders. Its 1.4-E2E-003 also expects a mono `↵ add` hint that QuickAddBar has never had, which was never implemented (a UX-DR6 gap, not a 3.4 regression). **Not verified in a browser:** list/region/heading semantics, the focus move after complete/delete, the drawer's focus trap and return, Escape in quick-add, and the placeholder color. Next step, once hydration is fixed: `npx playwright test tests/e2e/a11y-pass.spec.ts tests/e2e/quick-add-bar.spec.ts tests/e2e/keyboard-reorder.spec.ts tests/e2e/delete-confirm.spec.ts --project=chromium`.
- **Still open from 2.3:** the in-place delete confirm ("Confirm?" and its silent 2.5s revert) isn't announced to screen readers. This is 3.4's Question 2.
- **Test note:** Tailwind v4's `transition-colors` also transitions `outline-color`, so focus-ring assertions must poll (`expect.poll`) rather than read once. A one-shot read caught a mid-transition `rgb(139, 141, 155)`.

## Deferred from: dev-story of 3-5-responsive-and-accessibility-test-coverage (2026-09-23)

- **Production-mode Playwright unblocked the historical hydration hang.** The Story 3.5 chromium gate built and ran on `http://localhost:3100`; all axe scans and the main Add, Review & Complete, pointer-reorder, and BottomNav target checks executed. The prior 2.1–3.4 notes that task-dependent browser coverage could not run against the reused dev server are superseded by the `E2E_PROD=1` path added in Story 3.5.
- **Cross-section keyboard moves reset the remaining source-section order.** After moving C above A/B and then demoting A, Today becomes B/C instead of retaining C/B. `useTaskStore.moveTask()` rebuilds source positions from storage array order rather than current displayed/manual order. Covered by expected-failure guard `3.5-E2E-011` at both breakpoints; production logic is outside this test-only story's scope.
- **[Resolved by Story 3.5, 2026-09-23] Keyboard completion lost focus before the delayed removal.** `TaskRow` now captures whether focus was in the row before disabling the complete button, then performs the intended neighbour/quick-add handoff after the transition. `3.4-E2E-006` and `3.5-E2E-013` cover the fix.
- **Mobile secondary targets remain undersized.** TaskDrawer Save/Cancel measured 26px tall, and Completed's “View active tasks” measured 19.5px tall. The Story 3.5 scope explicitly forbids restyling these surfaces, so `3.5-E2E-021/022` remain visible expected-failure guards. Re-run them after a follow-up touch-target change and remove the markers when they unexpectedly pass.
- **WebKit pointer-drag automation remains flaky.** The desktop pointer journey passes in Chromium and Firefox, but WebKit's synthetic pointer sequence did not activate dnd-kit's PointerSensor in the serial production run. `3.5-E2E-014` is an expected failure on WebKit only; the keyboard journey remains the cross-browser accessible reorder gate.
- **WebKit needs constrained workers in this Windows environment.** Running the 20-spec Story 3.5 set together with Firefox at the default eight workers exhausted the Node/WebKit worker heap and closed browser contexts. A serial WebKit rerun (`--workers=1`) executed all 20 deterministically, isolating the single pointer-sensor flake above.
